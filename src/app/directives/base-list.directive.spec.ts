import { BaseListDirective } from './base-list.directive';

describe('BaseListDirective', () => {
  it('should create an instance', () => {
    const directive = new BaseListDirective();
    expect(directive).toBeTruthy();
  });
});
