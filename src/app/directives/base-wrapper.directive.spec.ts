import { BaseWrapperDirective } from './base-wrapper.directive';

describe('BaseWrapperDirective', () => {
  it('should create an instance', () => {
    const directive = new BaseWrapperDirective();
    expect(directive).toBeTruthy();
  });
});
